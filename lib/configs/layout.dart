import 'package:flutter/material.dart';

const EdgeInsets pagePadding =
    EdgeInsets.only(top: 20, left: 20, right: 20, bottom: 40);

const EdgeInsets pageVerticalPadding =
    EdgeInsets.only(top: 20, left: 0, right: 0, bottom: 40);

const EdgeInsets pageHorizontalPadding =
    EdgeInsets.symmetric(vertical: 0, horizontal: 20);

const EdgeInsets pagePaddingNoTop =
    EdgeInsets.only(top: 0, left: 20, right: 20, bottom: 40);

const EdgeInsets pagePaddingNoBottom =
    EdgeInsets.only(top: 20, left: 20, right: 20, bottom: 0);
